# 「事件簿：歷史教訓與情境推演」核實及發布準則

**版本：2026-09-17**

## 一、定位

事件簿以《史記》原文及三家註協助比較當代政治、歷史、法制、國際、社會或經濟事件。其產出是**歷史類比與可否證的情境推演**，不是事實預言、法律意見、投資建議、選舉宣傳或對特定政黨、候選人、選民的說服材料。

事件簿不得只輸出單一必然結論。所有分析均先標示為 **AI 初稿**，必須經真人編輯覆核，才可另行發布。

## 二、七道發布閘門

| 閘門 | 強制條件 | 系統證據 |
| --- | --- | --- |
| 當代事實 | 至少兩個不同網站、不同發布機構的來源；至少一個是一手來源 | 標題、機構、發布時間、查核時間、司法管轄區、網址、短摘錄及來源快照雜湊 |
| 《史記》證據 | 每項類比附篇名、資料層、原文引句及段落 ID | `volume`、`title`、`layer`、`section`、`quote`、`citation id` |
| 類比邊界 | 至少兩項相似、兩項差異及一項反例 | 結構化欄位；禁止把古今制度、主權、技術及社會條件直接等同 |
| 情境推演 | 30、90、365 日各一個情境 | 機率、命題、先行指標、失效條件及預先定義的結果判準 |
| 可追溯性 | 保存分析時間、模型、提示摘要、來源及提示雜湊 | `EVT-XXXXXXXXXXXX`、SHA-256、不可覆寫修訂鏈 |
| 事後驗證 | 到期後按原判準標記結果 | 已發生／未發生／無法判定、說明、核查時間、Brier score |
| 法律／政治揭露 | 明示工具性質及爭議來源分歧 | 每一分析均有揭露欄；介面固定提示 |

## 三、一手來源判定

可作一手來源者包括法律及命令原文、法院判決、政府公報、官方統計、立法或行政機關文件、當事機關正式文件。新聞稿是否屬一手來源，取決於其是否由事件當事機關發布；媒體轉述不因此成為一手來源。

「兩個來源」不是兩個網址而已。同一發布機構的不同頁面不構成獨立查核；系統同時檢查網站主機與發布機構名稱。來源若彼此矛盾，應在「來源分歧」欄清楚記載，不得挑選性隱藏。

## 四、分析與人工覆核流程

1. 編輯者輸入事件名稱、日期、司法管轄區、類型、已核實摘要、來源分歧，以及至少兩個合格來源。
2. 伺服器先執行來源數量、一手來源、網站與發布機構獨立性、網址、日期及摘錄長度檢查；未通過時不呼叫模型。
3. 系統跨《史記》一百三十篇原文與三家註檢索候選證據，將候選段落 ID 交給模型；模型不得自造引文 ID。維基文庫授權聲明、著作權文字、HTML 標記及匯出頁樣板不是史籍內容，必須在檢索與存檔兩階段排除。
4. 模型輸出須通過 JSON Schema、有效且確屬史籍內容的引文數量，以及 30／90／365 日完整性驗證。反例必須另填 `counterexampleCitationId`，該 ID 須同時存在於模型收到的候選段落與本次 `citationIds`；未能連回有效史籍段落時整份初稿拒絕存檔。
5. 系統產生事件簿編號，保存輸入快照、輸出快照、模型、分析時間、提示摘要與兩個 SHA-256 雜湊，狀態為 `AI 初稿`。
6. 管理員在 `/review` 逐項核查來源、引文、類比邊界、反例、機率、指標與結果判準；如有修改，原快照先寫入修訂表，不會被覆寫。
7. 管理員先按「完成編輯覆核」，使狀態成為 `編輯覆核`；再次確認內容沒有未保存變更後，才可按「發布已覆核版本」。
8. 已發布版本不可改寫原文字、機率、結果判準、模型或分析時間。若需更正，應建立新的事件簿或另加可辨識的修訂說明，不得回溯修改舊預測。

### 自動事件雷達

每日雷達以 Asia／Taipei 時區每 24 小時執行一次，研究過去 24 小時的重大公共事件，最多建立兩則通過閘門的 AI 初稿。雷達必須開啟完整來源頁，不得只依搜尋摘要；找不到兩個不同網站、不同發布機構且含一手來源的事件時，應回報沒有合格項目，不得補造。來源快照雜湊相同者會在模型呼叫前被拒絕，避免重複分析及不必要成本。雷達永遠不得自動進入編輯覆核或已發布狀態。

## 五、事後驗證及 Brier score

每個情境的結果判準在發布前即固定。到期後，管理員依新取得的一手及獨立來源，在校訂台選擇「已發生、未發生、無法判定」並填寫至少十字的核查說明。

二元結果的 Brier score 計算如下：

> `Brier = (預測機率 − 實際結果)²`

其中已發生為 1、未發生為 0；分數越接近 0，機率校準越佳。無法判定不計分。系統以百萬分之一為整數保存，再於介面轉回小數，避免浮點儲存差異。

讀者端 Artifacts 的結果按鍵只作**本機試算**，不會更新正式紀錄；正式結果只能由管理員在校訂台登錄。

## 六、資料表與權限

| 資料表 | 用途 | 寫入權限 |
| --- | --- | --- |
| `event_ledger_analyses` | 初始輸入、模型輸出、狀態、雜湊及時間 | 合格分析由伺服器建立；覆核狀態由管理員更新 |
| `event_ledger_revisions` | 每次修改前的完整內容快照 | 管理員覆核時由伺服器自動建立 |
| `event_ledger_outcomes` | 到期結果、說明及 Brier score | 只限管理員，且只接受已發布事件簿 |

公開查詢只回傳狀態為 `published` 的事件簿；AI 初稿及編輯覆核中的資料不會透過公開端點取得。

## 七、禁止事項

不得輸入未核實的指控並將其當成事實；不得利用事件簿作個人化政治說服、選民操控、違法歧視或針對個人的傷害預測；不得把《史記》的帝制、刑罰、戰爭或族群觀念直接移植成當代規範建議；不得省略重大反證、來源分歧或不確定性。
