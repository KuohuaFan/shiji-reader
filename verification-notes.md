# 視覺核驗紀錄

## 參考站

原《華嚴經》站採米色紙張背景、中央導讀首頁、橫向大分類、左側可展開目錄、中央逐篇閱讀、右側工具列與前後篇導覽。內容分為原文、自撰導讀／要義與本機對話層；語料可分片載入，也曾提供單檔自足版。原專案沒有獨立程式碼授權檔，README 並明示程式碼與版面為作者著作，因此本案僅採用資訊架構與內容編輯理念，未複製原始碼或版面素材。

## 《史記》桌機版（1440 × 900）

首頁的漢簡／朱砂印視覺、主標題、版本資料、五體入口及主要按鈕均完整呈現，無溢位或對比問題。一般篇章的左側五體目錄、中央閱讀紙張、右側工具列與標題層級均正常。表類篇章可顯示前言與年表，表格容器具水平捲動能力。

發現一項響應式細節：桌機寬度仍顯示行動版的漢堡與目錄關閉按鈕，原因是自訂 `.icon-button` 顯示規則覆蓋 Tailwind 的 `lg:hidden`。後續以明確的 `.mobile-only` 媒體查詢修正。
## 手機版與 DOM 核驗（390 × 844）

手機閱讀頁的頂部導覽、中央篇題、正文與固定底部工具列均完整，正文沒有水平溢位；目錄改為抽屜。首頁視覺完整但主要按鈕略低於首屏，已縮短手機版主視覺高度與區塊間距。實際 DOM 核驗確認 130 篇均可由目錄到達、首篇正文與來源連結完整。

DOM 核驗另發現 EPUB 個別卷首篇名有異稱、缺題與簡繁混用（例如第 18、25、27、44、79、89、92、126 篇）。轉換程式已改以維基文庫《史記》總目的一百三十篇標準篇名為唯一導覽標準，正文仍保留來源原貌。
## 互動核驗第一輪

重新匯入後，瀏覽器 DOM 已確認一百三十篇標準篇名全部顯示正確，先前的缺題與異稱均已消除。第一次使用自動化瀏覽器點擊頁首「檢索」後，工具未偵測到新面板且截圖為空白；目前無法判定是瀏覽器工具點擊索引失效、動畫時序，或前端執行錯誤，須重新檢視頁面狀態並以新的元素索引重測。
自動化瀏覽器以更新索引與可見座標各重試一次，頁面仍維持原狀且無前端錯誤訊息；同時 DOM、型別檢查與熱更新均正常。故改採本地端到端腳本驗證互動，以排除瀏覽器 review mode 對點擊事件的攔截。
## 最終視覺核驗

修正後桌機版不再顯示行動版漢堡與關閉按鈕；首頁、一般篇章與表類篇章在 1440 × 900 均無可見溢位、重疊或對比缺陷。手機首頁在 390 × 844 首屏可看見主標題、導言、統計與「開始閱讀」按鈕；手機閱讀頁的標題、正文和底部工具列保持清晰。

Chromium 端到端測試已通過十項檢查：首篇載入、全文檢索（「項羽」命中 27 篇）、結果跳轉與標示、本機書籤、夜讀模式、本機讀札、年表表格、手機目錄抽屜、手機底部工具列，以及瀏覽器主控台無錯誤。

## 第二版截圖工具狀態

全端升級並補裝依賴後，本機 `pnpm dev` 已在 `http://localhost:3000/` 正常啟動，TypeScript 無錯誤；但 WebDev 截圖工具仍沿用升級前管理層的舊錯誤狀態，兩次均回報「No preview URL available」，沒有產生圖片。因此不能據此判定新版畫面失敗；第二版視覺與互動改由本機 Chromium／Playwright 驗證，並將另存實際截圖供人工檢視。

## 第二版本機 Chromium 視覺核驗

以 1440 × 900 實際擷取三家註閱讀頁，確認五層分頁位於篇題與內文之間，當前層以朱砂底線清楚標示；集解、索隱、正義三色卡片對比足夠，左側目錄、中央紙張與七項右側工具列沒有重疊。以 390 × 844 擷取手機三家註頁，五個文字分頁可完整置於單列，三家註篩選按鈕與卡片不產生水平溢位；新增 AI 問答後七項固定底部工具仍可完整顯示，各標籤雖緊湊但可辨識。桌機與手機的標題、卷次、原文字數及資料層入口層級一致。

導讀層在 1440 × 900 下顯示清楚：AI 性質提示位於內容首段，篇旨使用較大宋體，內容概述與歷史脈絡以雙欄呈現，沒有欄位擠壓或截字。AI 問答抽屜約佔畫面四成寬，背景以遮罩保持上下文感；篇名、資料範圍提示、三個預設問題、輸入框與送出鍵都在首屏可見，沒有重疊或溢位。

## 第二版最終驗證

內容驗證通過 130／130 篇、5,015／5,015 個正文白話段落、1,610／1,610 個年表白話列、14,315 個三家註錨點與 29 則校勘註腳。單元測試 4／4 通過，正式建置成功，伺服器端 `dist/content` 的原文、三家註／校勘與導讀三層各有 130 檔。正式版另從 `/tmp` 而非專案目錄啟動，確認內容路徑不依賴工作目錄。

最終 Chromium 端到端測試通過 17 項：首篇載入、逐段白話、三家註分層、校勘註腳、篇旨人物、全文檢索、跳轉標示、書籤、夜讀、AI 問答及來源引用、讀札、年表、十表逐格白話、來源無注透明標示、手機目錄、手機底部工具列與主控台無錯誤。真實 `gpt-5-mini` 問答另通過答案、可信度及有效原文／三家註引用契約，並確認沒有模型特殊引用標記外洩至畫面。

## 第三版人物圖與年代軸視覺核驗

以本機 Chromium 在 1440 × 900 視窗擷取完整頁面。人物關係圖的六層分頁、關係類型篩選、人物搜尋、節點、關係線與人物詳情卡均可見；圖形維持漢簡紙色與朱砂強調，沒有超出中央閱讀紙張。人物詳情卡疊在右下角但不遮住分頁與篩選工具，並可捲動查看原文依據。

互動年代軸顯示事件類型篩選、水平時間線、事件卡、人物標籤和可展開的原文依據。卷一沒有可靠絕對年代時，介面誠實顯示「紀年未詳」，未虛構公元年份；水平容器可繼續捲動閱讀後續事件。

桌機錯誤回報以右側抽屜呈現，背景閱讀上下文仍可辨識；協作校勘說明、既有回報查詢、原文／白話選擇、問題類型、選取文字、修改建議、文獻依據與選填聯絡方式均在 1440 × 900 首屏可達，送出鍵狀態明確。

手機版 390 × 844 可完整顯示六個內容分頁、脈絡標題、人物圖／年代軸切換、搜尋與關係類型。人物畫布刻意保留水平捲動以維持節點可讀尺寸；八項底部工具列（含問答與回報）均可見且沒有互相覆蓋。

## 第三版互動驗證

Chromium 端到端測試通過 21 項：首篇載入、逐段白話、三家註、校勘、導讀、16 個卷一人物節點、10 個卷一年代事件、全文檢索、檢索跳轉、書籤、夜讀、只引用索隱且限定上古時期的 AI 問答、帶入選取原句的側欄回報、讀札、年表、十表白話、來源無注透明標示、手機目錄、八項手機底部工具、校訂台登入閘門及主控台無錯誤。

## 第三版最終發布驗證

人物脈絡資料完成 130／130 篇，共 1,897 個人物、1,493 條關係、1,310 個事件及 51 個具絕對年份事件。全量驗證確認沒有不存在的人物端點、重複人物、異常年份或無法在本篇原文回查的關係／事件引句。

原文與既有學術層再次通過 130 篇、733,130 字、5,015 段白話、1,610 列年表、14,315 個三家註錨點與 29 則校勘註腳。TypeScript、5 個單元測試、正式建置、真實模型指定索隱來源測試、真實資料庫回報／修訂／簽核／清理測試均通過。正式打包版本從 `/tmp` 啟動後，21 項 Chromium E2E 再次全部通過；robots 與含 131 個 URL 的 sitemap 均可讀取。測試完成後資料庫回報、篇章簽核與修訂表均為 0 筆，沒有把自動化測試冒充實際人工校訂。

## 第四版對話工作臺視覺核驗

1440 × 900 Chromium 截圖確認首頁為滿版、無訊息氣泡、無置中卡框的沉浸式對話畫面。左欄固定呈現紀錄、Starred、Projects、Artifacts；中央以大幅留白、史印與問題建議作為對話起點；上方第一列是章節、古今書庫、事件簿，第二列是七道發布閘門；下方輸入列包含模式、章節、書籍與時期控制。

事件簿右側抽屜約佔桌機畫面三成，中央對話區同步縮窄而不被遮住。事件名稱、管轄區、日期、分類、已核實事實、來源分歧及來源欄位均使用底線式編輯，維持無卡框設計；表單可獨立垂直捲動。上方「當代事實」閘門會直接開啟右欄並向下展開核實內容。

390 × 844 手機截圖確認左欄收合後，章節、古今書庫與事件簿保留在上方；七道閘門以圖示橫列；中央標題、四個建議與下方輸入區皆無水平溢位。對話輸入、模式、章節與書籍按鍵保持在首屏可用範圍。

桌機七道閘門抽屜確認每一項都可單獨點擊並向下展開。尚未建立事件簿時，當代事實閘門誠實顯示不會以未核實資料產生預測；其餘閘門維持折疊摘要，沒有卡片堆疊或資訊重疊。

## 第四版最終發布驗證

第四版發布閘門已完成：內容為 130／130 篇、733,130 字；正文白話 5,015／5,015 段、十表白話 1,610／1,610 列；三家註 14,315 錨點、校勘註腳 29 則；脈絡資料 1,897 人物、1,493 關係、1,310 事件，全部關係端點及證據引句通過。

單元測試 9／9 通過。事件簿資料庫 E2E 通過 AI 初稿、編輯覆核、另行發布、已發布不可改寫、原始版本保留、公開查詢限制、30 日結果與 Brier score 0.160；測試資料均已清理。真實 `gpt-5` 測試通過兩個獨立來源、一手來源、跨篇史記引文、類比差異、反例、30／90／365 日情境、事件簿編號及兩個 SHA-256 雜湊，完成後自動刪除測試草稿。

正式建置成功；從 `/tmp` 啟動 production 服務後，Chromium 27／27 項通過，涵蓋滿版無氣泡入口、四個左側工作區、章節與書庫、七道閘門、事件表單、Artifacts 核實檔案、本機 Brier 試算、原讀本六層內容、AI 注家與時期篩選、讀者回報及手機版，瀏覽器主控台無錯誤。robots 與 131 個 sitemap URL 亦通過。

每日自動事件雷達已建立並核對為 `active`、`isEnable: true`、Asia／Taipei、間隔 86,400 秒、standard 模式。排程最多研究兩件過去 24 小時事件，必須開啟完整來源、通過兩網站／兩機構／一手來源門檻，只建立 AI 初稿且不自動發布；相同穩定來源快照會在模型呼叫前去重。
排程連接器已依最小權限縮減為 GitHub；Gmail、Google Calendar、Google Workspace、Canva 及 Resend 均已從每日雷達排程移除。雷達研究依靠內建公開網頁搜尋與來源擷取，不讀取使用者郵件或日曆。

## 第四版沉浸式對話動效核驗

助理回答新增標點感知的漸進打字效果，長回答會自動調整每批字數，避免等待過久；打字期間提供可鍵盤操作的「顯示全文」，引用與資料支持度在全文完成後出現。螢幕閱讀器可立即取得完整答案，不必等待視覺動畫。

智慧捲動只在讀者位於最新訊息附近時跟隨回答；偵測到向上捲動後立即停止追隨並顯示「回到最新對話」。按下後平滑返回底部。`prefers-reduced-motion: reduce` 實測為 `scroll-behavior: auto`，動畫時長壓至 0.01ms，打字機立即顯示全文。

Chromium 桌機 1440 × 900 與手機 390 × 844 的打字中途截圖確認：逐字游標、顯示全文控制、講者標示及輸入列均無重疊或水平溢位。完整端到端測試增至 30 項，新增「工作臺打字機與立即顯示全文」、「智慧平滑捲動與閱讀位置保護」及「降低動態偏好」，全部通過。

## 回訪首頁與保存紀錄分離核驗

使用者截圖顯示根網址直接停留在卷 24 舊對話。核實原因是工作臺初始化時自動把 `localStorage` 最新紀錄指定為目前對話，並非首頁版面遺失。現已改為根網址每次建立獨立空白首頁；保存紀錄只列於左欄，由使用者主動恢復，空白首頁不寫入紀錄。

以預先保存卷 24 對話的瀏覽器狀態，在桌機 1440 × 900 與手機 390 × 844 重新開啟 `/`，兩者均顯示「以古鑑今，先問證據」完整首頁。點入舊紀錄後內容正常恢復；按上方首頁圖示可返回首頁，紀錄仍在。Production E2E 因新增「回訪首頁與舊紀錄分離」增至 31 項。

## 2026-09-14 事件簿自動雷達

本輪窗口為 2026-09-12T17:21:48Z 至 2026-09-13T17:21:48Z。完整開啟並交叉核實來源後，建立兩則未發布 AI 初稿：`EVT-8EDA10C669AA`（印尼 KMP Virgo Transport 8 翻覆及搜救）與 `EVT-278704BDA6CD`（瑞典 2026 國會、區域及市政選舉）。每則各有三個不同網站及發布機構的來源、其中一個為一手官方來源，並含有效《史記》引文、類比邊界、反例及 30／90／365 日可否證情境。

瑞典事件第一次模型結果 `EVT-83665FE1559F` 曾誤選維基文庫公有領域聲明為《史記》原文。該筆從未覆核或發布，且無修訂與結果紀錄，已刪除。檢索層現會排除公有領域、著作權、維基文庫及匯出頁樣板；事件簿存檔前再次驗證引文。重建後所有引文均為可回查史籍內容。

資料庫唯讀驗證確認兩則最終資料均為 `ai_draft`，修訂數與結果數皆為 0。相同瑞典來源快照再次呼叫時，在模型前被拒絕並返回既有事件簿編號，證明去重生效。TypeScript 檢查及 10 項單元測試通過。

## 2026-09-15 事件簿自動雷達

本輪窗口為 2026-09-13T17:21:39Z 至 2026-09-14T17:21:39Z。完整開啟並交叉核實來源後，建立兩則未發布 AI 初稿：`EVT-E4020FD2208A`（瑞典大選初步計票與組閣不確定性）及 `EVT-2B3CA43AD3C3`（中國《緊急採購管理暫行辦法》）。兩案各有三個來源、至少一個直接一手來源、不同網站與不同發布機構；各含可回查的《史記》段落 ID、類比差異、反例及 30／90／365 日可否證情境。

資料庫唯讀稽核確認兩案均為 `ai_draft`，真人覆核、發布時間、修訂版本與事後結果皆為空或 0。以相同來源快照重跑時，路由在模型呼叫前回傳既有事件簿編號，未重複送模。EPA 發電廠碳排規則與歐盟制裁七日延長因本輪截止時沒有找到可直接開啟的官方終局文件，均未送入事件簿。
## 2026-09-16 事件簿自動雷達

本輪窗口為 2026-09-14T17:21:44Z 至 2026-09-15T17:21:44Z。逐頁開啟並交叉核實來源後，建立兩則未發布 AI 初稿：`EVT-ABB41B4BF7E7`（美國 EPA 部分撤銷 2024 年電廠碳排規則）及 `EVT-D55E8CEA74F2`（中國第 841 號令出境入境管理規定施行）。兩案各有三個來源、兩個一手來源、至少兩個不同網站及不同發布機構；共含七筆可回查的《史記》段落 ID、類比差異、反例及 30／90／365 日可否證情境。

資料庫唯讀稽核確認兩案均為 `ai_draft`，真人覆核、發布時間、修訂版本與事後結果皆為空或 0。全部引文都有篇名、資料層、原文與段落 ID，且沒有維基授權或頁面樣板文字。以相同來源快照重跑時，路由在模型呼叫前分別回傳既有事件簿編號，未重複送模。臺灣兩岸運動交流規範因官方修正及新聞公告時間均早於本輪窗口而未送入事件簿。

## 2026-09-16 開源與 GitHub Pages 視覺核驗

GitHub Pages 產物在 `/shiji-reader/` 子路徑啟動後，桌機首頁成功載入 512 × 512 紫色鳥形 Logo；左側品牌、中央主視覺、章節／書庫／事件簿導覽與下方輸入區均無溢位。卷一可由 `?chapter=1` 直達，讀本頁首亦顯示 Logo，返回首頁後仍保留 `/shiji-reader/` 子路徑。

首次手機截圖顯示左側工作抽屜預設展開，遮住中央首頁大部分內容。這不是資產或子路徑錯誤，但不符合手機沉浸式首頁的預期；已列為本輪立即修正項目，修正後將重新截圖及執行端到端測試。

手機左側工作抽屜已改為 820px 以下預設收合。390 × 844 複核截圖確認 Logo、首頁主標題、四個建議、上方章節／書庫／事件簿與下方輸入區完整可見，沒有遮擋或水平溢位；漢堡按鈕可展開側欄。Pages 專用端到端測試另確認 512 × 512 Logo、`/shiji-reader/about`、正式站 `/review`、`?chapter=1` 直達與讀本返回首頁路徑均正確。

GitHub Pages 正式網址實測已回傳 HTTP 200。另因正式全端網域目前尚未載入新 CORS 版本，Pages 已改為不在背景呼叫正式 API：開啟白話層的外部 API 請求為 0；AI、事件簿與校勘提交會明確導向正式全端網站。桌機與 390 × 844 手機端到端測試均確認 Logo 為 512 × 512、子路徑正確、靜態讀本完整且導向網址保留問題文字。

公開倉庫已建立於 `https://github.com/KuohuaFan/shiji-reader`，`main` 保存完整開源原始碼與 MIT 授權，`gh-pages` 只保存可發布靜態產物。正式 Pages `https://kuohuafan.github.io/shiji-reader/` 已回傳 HTTP 200；公開網址桌機與手機 Chromium 測試均通過：512 × 512 Logo 載入、首頁與卷一讀本可開啟、白話層不發送正式 API 請求、AI 送出明確導向正式站、404 fallback 可載入應用，sitemap 共 131 個 URL。

## 2026-09-17 事件簿自動雷達

本輪窗口為 2026-09-15T17:21:25Z 至 2026-09-16T17:21:25Z。逐頁開啟並交叉核實來源後，建立兩則未發布 AI 初稿：`EVT-BF889A2F81AD`（香港首個五年規劃，3 個不同網站／機構來源，其中 1 個一手來源）及 `EVT-C993154CC164`（美國參議院 CLARITY 數位資產法案程序表決受阻，4 個不同網站／機構來源，其中 2 個一手來源）。兩案分別保存 9 及 2 個有效《史記》引文，且反例專屬引文為 `v102:o-0-5` 及 `v084:o-1-2`；30／90／365 日情境、機率、先行指標、失效條件及預先結果判準均完整。

本輪發現三家註資料中的維基匯出標頭與 HTML 殘片可能進入候選證據，已加固檢索與存檔雙重閘門，排除 `reader|title=`、`data-mw`、`typeof=`、HTML、授權及著作權樣板；另新增 `counterexampleCitationId`，反例未連回有效且同時列入 `citationIds` 的史籍段落時整份初稿拒絕存檔。三筆強化前且未覆核的瑕疵草稿已安全撤回。資料庫唯讀稽核確認兩筆最終分析均為 `ai_draft`，`reviewedBy`、`reviewedAt`、`publishedAt` 為空，修訂與結果各為 0；相同來源快照重跑均在模型呼叫前回傳既有編號。型別檢查及 11 個單元測試通過。

## 2026-09-18 事件簿自動雷達

本輪窗口為 2026-09-16T17:21:43Z 至 2026-09-17T17:21:43Z。逐頁開啟並交叉核實四項候選來源後，依最多兩項限制建立兩則未發布 AI 初稿：`EVT-C3FDD0653BA4`（美國聯準會升息一碼至 3.75%–4.00%，三個來源，其中兩個一手來源）及 `EVT-E9057CD37EA6`（聯合國調查團認為美軍兩次伊朗空襲可能構成戰爭罪，三個來源，其中一個一手來源）。澳洲臨時移民改革與美國政府採購排除加拿大產品措施雖通過來源門檻，但因本輪上限未送入模型。

資料庫唯讀稽核確認兩案均為 `ai_draft`。`reviewedBy`、`reviewedAt` 與 `publishedAt` 為空，修訂及結果各為 0；兩案分別保存 3 及 5 個有效《史記》段落，反例引文為 `v006:o-1-3` 與 `v040:o-7-0`。30／90／365 日情境、主觀機率、先行指標、失效條件及預先結果判準均完整。全部引文掃描未見維基授權、頁面樣板、HTML 或匯出殘片。以相同來源快照重跑時，路由在模型前分別回傳既有事件簿編號，證明去重生效。TypeScript 檢查、11 項單元測試及正式全端建置均通過。

## 2026-09-19 事件簿自動雷達

本輪窗口為 2026-09-17T17:21:45Z 至 2026-09-18T17:21:45Z。逐頁開啟並交叉核實四項候選後，依最多兩項限制建立兩則未發布 AI 初稿：`EVT-3A588CF7B80D`（美國批准一項可能對沙烏地阿拉伯出售 48 架 F-35 及配套，兩個來源，其中一個一手來源）及 `EVT-6AAC12637E50`（臺灣電價費率審議會暫不決定調價並延至 12 月再議，兩個來源，其中一個一手來源）。臺灣 115 年度中央政府總預算正式公布亦通過來源閘門，但因本輪上限未送入模型；瑞典首相辭職案因官方頁面無可核實時分或時區、可核實的 Reuters 首發又早於窗口，未通過時窗閘門。

資料庫唯讀稽核確認兩案都是 `ai_draft`，`reviewedBy`、`reviewNote`、`reviewedAt` 及 `publishedAt` 均為空，修訂與事後結果各為 0。兩案分別保存 2 與 3 個有效《史記》段落，反例引文為 `v123:o-2-6` 與 `v126:o-5-1`；30／90／365 日情境、機率、先行指標、失效條件及預先結果判準均存在。全部引文未見維基授權、著作權、HTML 或匯出樣板殘片。相同來源快照重跑時，路由在模型前回傳原事件簿編號，證明去重有效。
